# Chapter 6 — Conclusion

This thesis investigated which stages of the standard micro-expression recognition pipeline are responsible for its performance. It treated that pipeline as a factorial experiment rather than as a proposal: four components — Eulerian magnification, a convolutional spatial stem, parameter-free attention, and a temporal transformer — varied independently across twelve configurations covering every architecturally valid cell of the matrix, each trained from scratch and scored under complete 25-fold leave-one-subject-out on CASME II, with every non-varied factor held identical.

This chapter closes the account. §6.1 states what the study contributes; §6.2 sets out the limits within which those contributions hold; §6.3 sets out the work the design leaves undone, including the commitments made to it earlier in the thesis; and §6.4 says what a reader should take away.

---

## 6.1 Contributions

§3.10.3 states the gap this study closes: the field has settled on a standard pipeline for micro-expression recognition but has never measured which of its stages are responsible for the result, leaving a practitioner no basis for allocating effort. §4.1.1 puts the operative question accordingly — not how well the pipeline performs, but which of its stages are responsible for that performance, and by how much. The thesis answers it. Four contributions follow, in descending order of what each is worth.

- **A matched-pair factorial measurement of the standard pipeline.** The reviewed corpus evaluates components only inside complete systems, so an improvement can seldom be attributed to the part credited with it (§3.10.2). One reviewed study does toggle a single stage against an otherwise fixed pipeline (§3.2.3), but that stage is a preprocessing step. No reviewed study isolates a *learned* component, whether a convolutional backbone, an attention module or a temporal encoder, against an otherwise identical pipeline, and none varies several jointly so that their interactions can be read. Twelve configurations cover every architecturally valid cell of a 2⁴ matrix, each trained from scratch and scored under complete 25-fold leave-one-subject-out with every non-varied factor held identical (§4.1.1, §4.3).

- **The proposed unified system is not this study's best configuration.** `config_2_temporal_only`, a temporal transformer applied to the motion tensor alone, reaches **0.7122** pooled macro F1 against `config_8_proposed_unified`'s **0.6659** (§5.1.2); and the component consuming most of the sweep's compute is the only one whose mean effect is negative (§5.5.4, §5.8.1). This is the thesis's most useful finding, and the design produced it rather than conceding it. An end-to-end study reporting the proposed system alone would have published the lower of those two numbers, with no way to discover that a subset of its own architecture outperformed it.

- **The shape of the effect distribution across the four components.** The contribution is not any individual mean but the form of the distribution: of the effect this design measures, almost all concentrates in one component, a second is harmful on average, a third cannot be distinguished from noise at this sample size, and the fourth has no settled direction across its pairs (§5.8.1). §5.8.2 makes the structural point — an end-to-end evaluation returns a single number and so cannot exhibit a distribution at all.

- **A documented account of what was excluded.** §4.6.7 records four earlier evaluations of the same matrix and the property of each that disqualifies it: a data-routing defect, degenerate class support, incomplete fold or configuration coverage. Reporting one complete protocol rather than the most favourable of five is itself a methodological position, and one the reviewed corpus rarely documents (§3.10.2).

---

## 6.2 Limitations

§5.8.3 gathers the limitations bearing on the results themselves, and its closing bullet — that each component is varied only as present or absent — is the hinge into this section. What follows is what no single chapter gathers: the limits of the study as a whole, and what the fixed settings behind that bullet cost.

**The ablation measures architecture at one point in preprocessing space.** §3.10.4 records that α = 10, T = 32, λ = 10⁻⁴ and γ = 2.0 are inherited from the literature and held fixed throughout, never swept, even though §3.10.2 establishes that such parameters have interior optima locatable only by exhaustive search. Every effect in Chapter 5 is conditional on those values, and this design cannot say how any would move if they changed.

**Twelve declared divergences from the methods reviewed.** §3.10.5 ledgers them; three bear directly on how the results should be read. The convolutional stem carries no temporal kernels, so what was measured is a learned *spatial* stem rather than spatio-temporal convolution (§3.6.6). It runs at 224 × 224 against a literature norm at or below 100 × 100, so its negative effect may be a resolution result as much as an architectural one — a reading §5.5.5 advances and then qualifies, since the same source reports shallow models to be largely robust to resolution. And magnification is applied after subsampling, so its realised pass-band is clip-dependent and the component was measured outside its intended specification (§3.5.7). None invalidates the ablation — every configuration shares them — but each narrows what the corresponding result is a result *about*.

**One corpus, one demographic, trained from scratch, with no composite training.** §3.1.5 states the cost of that choice directly: no claim in this thesis extends to other populations, other capture conditions, or other corpora. §3.10.4 is explicit on this point: what these components do on a single small corpus without pre-training is a separate question from how they behave under composite-database training, not an answer to it, and no cross-dataset claim is made anywhere.

These three bound what the study is valid for. The measurements report what four components contribute to this pipeline, on this corpus, at one fixed setting of the preprocessing parameters, under the divergences §3.10.5 records. Within that scope they are matched-pair measurements against a single complete protocol; outside it they are not evidence, and nothing in Chapter 5 is offered as though they were.

---

## 6.3 Further Work

§5.8 closes by asking what a stronger design would still need to establish. The answer falls into three kinds: changes that would make this study's numbers testable, experiments its own findings point at, and the backlog of departures §3.10.5 ledgers.

**Making the numbers testable.** Three changes repair the evaluation rather than the model, and none requires an architectural change. The first is an inner validation split. §4.6.2 records that checkpoint selection and final scoring use the same held-out fold, and §5.7.4 identifies this as the one respect in which the figures reported here are more favourable than the protocol supports. Selecting the checkpoint on subjects withheld from the training fold, and scoring the test subject once only, is the single change that would make every figure in Chapter 5 trustworthy, and demands no architectural change. The second is retaining per-clip predictions, which §4.6.6 records are not stored. Keeping them costs nothing and converts every comparison in Chapter 5 from a difference between point estimates into one a paired test — McNemar's, over the clips on which two configurations disagree — could adjudicate. The third, and the most generic, is repeating the sweep under several seeds. §4.1.5 fixes the design at a single seed and §5.2.3 states precisely what that costs: six same-direction matched pairs evidence a *sign*, not a magnitude. Multi-seed runs are what would attach an interval to a quantity this design can only order.

**What the findings point at.** Of the experiments the findings themselves motivate, §3.6.6 puts one first, and it is the clearest unforced departure from the literature: the stem arm re-trained at 28 × 28 and at 56 × 56, inside the range the reviewed work converged on, with everything else held as Chapter 4 specifies. It is the cheapest experiment left undone and the one whose outcome would most change how §5.5's result must be worded. Second, magnification should be applied before subsampling rather than after, so that the realised pass-band is the 5–25 Hz band §3.2.7 derives rather than a clip-dependent fraction of it (§3.5.7). Third, the three improvements §3.8.6 draws from its own divergence table, in that section's order: replacing mean pooling with an LSTM aggregator, the variant SLSTT's own ablation favours (§3.8.4); recomputing the input as long-term onset-referenced flow rather than consecutive-frame flow, a Stage 1 change costing no training time; and replacing the fixed sinusoidal encoding with learned position embeddings, at a negligible parameter cost. None of the three was tested here, and none disturbs the ablation's conclusions. Fourth, the stem itself: §5.5.5 sets out three candidate explanations for its behaviour — a spatial-only kernel, an input that already encodes motion analytically, and a resolution far above the literature's — and states that this study cannot adjudicate between them. Varying kernel shape, input resolution and the stem's position relative to the flow computation independently of one another is the design that would.

**The declared-divergence backlog.** Read forwards, the departures this review records are themselves a list of experiments. Two come from §3.3.7 rather than from the ledger: flow-based fine alignment, which that section names the most promising unexploited improvement and which this pipeline forgoes, and Farnebäck against TV-L1 as the estimator. From §3.10.5's twelve divergences come the dedicated strain post-filter this pipeline omits, and the four-term strain magnitude, whose √2 shear weighting the three-term form declared in §3.4.3 does not carry. The inherited preprocessing parameters are the other half of the backlog: sweeping T (§3.5.7), and sweeping α, λ and γ (§3.10.4), would establish whether the effects reported here survive away from the single point at which they were measured. A strain-on/strain-off arm, which §3.4.7 names the natural next ablation, would extend the matrix to the one input channel this design holds constant. Attention carries its own short list. §3.7.8 names three follow-ups, all cheap because the module trains no parameters: whether SimAM's distinctiveness prior suits feature maps derived from flow rather than from pixel intensity, what re-tuning λ would do, and the untested two-dimensional against three-dimensional pooling variant §3.7.7 decides without measuring. §3.7.7 also leaves retention itself open — the component is cheap rather than free (§5.4.5), and whether that price is worth an effect indistinguishable from noise is a question a larger evidence base would settle. Two further arms are cheap additions to the matrix rather than new machinery: focal loss against plain cross-entropy, and the values of γ and of label smoothing, all three fixed throughout this study (§3.9.8). §3.9 records two topics excluded from the implemented system that belong to any fuller account of this pipeline — Grad-CAM++ visual auditing, which would show whether the transformer attends where the action units are, and adversarial identity disentanglement, which bears directly on the single-demographic limitation §6.2 states.

Beyond the matrix, the corpus itself is the binding constraint. §3.1.5 states the cost of remaining single-corpus — that no claim here extends to other populations, other capture conditions or other corpora — and a composite-database evaluation of the kind §3.1.6 describes is the direct test of whether any effect reported here survives outside CASME II. One item, finally, is not an experiment but a safeguard: §3.2.7's verification helper should be wired in as a start-up assertion, so that a routing defect of the kind it was written to catch cannot recur silently.

---

## 6.4 Final Remarks

This thesis asked a narrow question of a standard pipeline. Not how good a score it can be made to produce, but where inside it that score comes from. It answered by building every architecturally valid combination of four components and scoring each under one complete leave-one-subject-out protocol, so that each component could be read off matched pairs differing in nothing else. The plainest statement of what came back is that the stages do not contribute equally, that what a stage costs is a poor guide to what it returns, and that the configuration a designer would have proposed was not the configuration that scored best.

§3.10.3 puts the practical complaint directly: a practitioner assembling this pipeline today has no basis for allocating effort. This study supplies one — a matched-pair account of what each of the four stages contributes, in which one carries almost all of the measured effect, one subtracts on average while consuming most of the compute, and two cannot be separated from noise at this sample size. That is a coarser instrument than a ranking, and it is the one the evidence supports. Delivered with an explicit statement of the corpus, the protocol and the fixed preprocessing setting for which it holds, it is a more useful thing to hand a practitioner than a headline score with none.

The design is the more durable contribution. Building the matrix rather than the proposal is what made that ordering visible at all, and the experiments §6.3 sets out are ones this study is in a position to specify precisely because it was built that way.

---

