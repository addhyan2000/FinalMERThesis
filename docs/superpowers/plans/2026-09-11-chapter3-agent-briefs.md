# Chapter 3 Reformatting — Agent Briefs

Four parameterised prompts, adapted from `AGENT_PROMPTS.md` for **editing already-verified text** rather than writing new text. Replace every `{{PLACEHOLDER}}`.

Companion to `2026-09-11-chapter3-reformat.md`. Convention sheet C1–C10 lives in `../specs/2026-09-11-chapter3-reformat-design.md`, section S2.

---

## Shared preamble — paste at the top of every brief

```
**Project root:** `/Users/addhyanpant/Desktop/Thesis5/FinalMERThesis` (run commands from here)

**PDF extraction:**
`{{VENV}}/bin/python {{SCRIPT}}/ex.py "docs/<file>.pdf" [max_pages]`
PDF extraction glues table columns and mangles maths — re-read the surrounding lines
before declaring a number wrong.

**CRITICAL METRIC WARNING.** `Ablation_Study/results/config_*/final_results.json`
contains TWO macro-F1 quantities that can differ IN SIGN:
- key `macro_f1` = MEAN-OF-FOLDS. The thesis explicitly rejects this (capped at 0.6267).
- POOLED macro F1 = mean of the `per_class_f1` array. **This is the primary metric.**
Always compute pooled unless the text says otherwise. Report both if they differ.

Note: the `docs/` file `CASME2_ADatabaseofSpontaneous...pdf` is Qu et al.'s **CAS(ME)²**
paper, not CASME II — do not report it missing.

**THIS SECTION IS ALREADY FACT-AUDITED AND REFERENCE-AUDITED.** You are not
re-verifying its claims. Any factual change is a defect, INCLUDING ONE THAT LOOKS
LIKE A CORRECTION. If you believe a fact is wrong, report it — do not fix it.

**NEVER EDIT `00_Chapter3_Complete.md`.** It is generated by `rebuild_complete.sh`
from the numbered section files.

**NO GIT COMMITS.** All work stays in the working tree.

**THE CONVENTION SHEET (C1–C10)** is the contract for this task. Read
`docs/superpowers/specs/2026-09-11-chapter3-reformat-design.md` section S2 in full
before doing anything. Summarised:
- C1  in-corpus citations are numeric `[n]`, author name kept in narrative prose
- C1e out-of-corpus attributions keep author–year and get NO bracket
- C2  chapter preamble is unnumbered framing prose (handled centrally, not by you)
- C3  no blockquotes — `> **Scope of this section.**` becomes ordinary paragraphs
- C4  numbered subsections KEEP their numbers; bold unnumbered sub-subheads added inside
- C5  figures captioned `*Figure 3.n — …*`, referenced in prose before they appear
- C6  footnotes for term definitions that currently interrupt the argument
- C7  a bridging sentence closes each section, pointing at the next
- C8  §3.10 is the chapter's conclusion; its NUMBER does not change
- C9  table captions already correct — no change
- C10 every fact, number, quotation, table, divergence, gap statement and subsection
     number is UNCHANGED
```

---

## Brief 1 — Format audit *(report only)*

> Senior academic editor auditing one section of a completed MSc thesis literature review against a fixed convention sheet. **Report only — do NOT edit anything.**
>
> {{SHARED PREAMBLE}}
>
> ## The section
> `{{PATH}}` — §{{N}} "{{TITLE}}", {{WORDCOUNT}} words.
>
> ## Your job
> Identify every deviation from C1–C10, and nothing else. You are not improving the prose, not restructuring, not re-arguing. A sentence that is merely long or plain is not a finding.
>
> Report, each with a line number and the text quoted:
>
> 1. **C1 / C1e — citations.** Any in-corpus citation still in author–year form. Any out-of-corpus attribution that has wrongly acquired a bracket. Any `[n]` whose author name does not match the reference-list byline for that number in `11_References.md`.
> 2. **C3 — blockquotes.** The `> **Scope of this section.**` block and any other blockquote. Give the exact prose it should become — same words, blockquote markers removed, bold lead-ins retained.
> 3. **C4 — bold sub-subheads.** Which subsections are long enough to want them, and the exact heading text proposed for each. Prefer promoting bold lead-ins that already exist in the text over inventing new ones. Name the line where each goes.
> 4. **C5 — figures.** {{FIGURE_INSTRUCTION}}
> 5. **C6 — footnotes.** Parenthetical definitions that interrupt the argument and should become Markdown footnotes. Quote each and give the footnote text, which must be the existing words unchanged.
> 6. **C7 — bridge.** The closing bridge sentence: where it goes, and what it should say. It points at §{{NEXT_SECTION}} ({{NEXT_TITLE}}). It must be assemblable from material already in this section — you may not introduce a new claim to build a bridge.
>
> ## Output
> - **VERDICT: n changes required**
> - One block per category C1, C1e, C3, C4, C5, C6, C7. **Say explicitly if a category is empty** — silence is ambiguous.
> - For each finding: line number, text as written, exact replacement text.
>
> Do not propose any change to wording that is not one of C1–C10. If you think the section has a problem outside the convention sheet, put it in a final **OUT OF SCOPE — NOTED** block and take it no further.

---

## Brief 2 — Format writer *(edits exactly one file)*

> Senior academic editor applying an approved format audit to one section of a completed MSc thesis literature review. **You will edit exactly one file.**
>
> {{SHARED PREAMBLE}}
>
> ## Your file
> `{{PATH}}` — and no other. Do not touch any other section, `11_References.md`, `rebuild_complete.sh`, or `00_Chapter3_Complete.md`.
>
> ## What to apply
> {{FORMAT_AUDIT_REPORT}}
>
> ## Hard constraints
> - **Change no fact, number, quotation, table cell, citation target, section number, section title, gap statement or verification stamp.**
> - The **only** new prose you may write is the closing bridge sentence and, where the audit calls for it, an opening paragraph converted from the blockquote. Both must be assemblable from material already present in this section. You may not introduce a claim, a hedge, a comparison or an evaluation that is not already in the text.
> - Bold sub-subheads must use words already in the text where possible.
> - Footnote text must be the existing parenthetical words, unchanged.
> - Do not merge, split, renumber or retitle any subsection.
> - Do not reflow or rewrap paragraphs you are not otherwise changing — it makes the diff unreadable for the audit that follows.
>
> ## Report back
> - Word count before and after.
> - An itemised list of every change, by convention (C1, C3, C4, C5, C6, C7).
> - The exact text of any new prose you wrote, quoted in full, so it can be checked.

---

## Brief 3a — No-drift audit *(report only — the load-bearing one)*

> Auditing a formatting edit to a fact-verified thesis section. Your single job is to prove that **nothing but formatting changed**. **Report only — do NOT edit.**
>
> {{SHARED PREAMBLE}}
>
> ## Inputs
> - File: `{{PATH}}`
> - Diff: run `git diff -- {{PATH}}` from the project root. The file is uncommitted, so this shows every change made in this task.
> - The before-text is the version at `HEAD`: `git show HEAD:{{PATH}}`
>
> ## Your job
> Enumerate **every** hunk in the diff. Classify each one as exactly one of:
>
> - `C1` numeric citation substitution
> - `C3` blockquote converted to ordinary prose
> - `C4` bold sub-subhead added
> - `C5` figure inserted or referenced
> - `C6` parenthetical converted to a footnote
> - `C7` bridge sentence added
> - `NEW-PROSE` an opening paragraph or bridge — quote it in full and state what in the before-text it was assembled from
> - **`DRIFT`** anything else
>
> Then verify positively, not by absence:
>
> 1. **Every number** in the before-text appears unaltered in the after-text. List them and tick them off. This includes figures inside tables, inside equations, and inside citations.
> 2. **Every double-quoted passage** in the before-text appears verbatim in the after-text.
> 3. **Every table** has the same number of rows and the same cell contents.
> 4. **Every `### 3.x.y` heading** has the same number and the same title.
> 5. **The verification stamp** in the footer is unchanged.
> 6. **Word count** has not fallen. It may rise by the length of the new prose.
>
> ## Output
> - **VERDICT: NO DRIFT** or **DRIFT (n)**
> - A table: hunk location → classification → one-line justification
> - **DRIFT** findings quoted in full, before and after
> - **NUMBERS CHECKED** — the list, with any mismatch called out
> - **QUOTATIONS CHECKED** — the list, with any mismatch called out
> - Say explicitly if a category is empty.
>
> Do not judge whether the formatting is good. Judge only whether the content survived.

---

## Brief 3b — Citation audit *(report only)*

> Auditing the citations of one thesis section after a conversion from author–year to numeric form. **Report only — do NOT edit.**
>
> {{SHARED PREAMBLE}}
>
> ## Inputs
> - File: `{{PATH}}`
> - Reference list: `Thesis_Chapter3_LiteratureReview/Tech-wise/11_References.md` — 27 entries, each prefixed `[n]`
> - Citation map: `Thesis_Chapter3_LiteratureReview/Tech-wise/citation_map.tsv`
>
> ## Your job — in this order
> 1. **Every `[n]` in the section resolves** to an entry in `11_References.md`. Any that does not is a **GHOST REFERENCE** — report it prominently.
> 2. **Every name-plus-number pairing is correct.** Where the text reads `Yan et al. [23]`, entry 23 must have Yan as first author. Check against the reference list's own byline, **not against your prior knowledge**. This check caught OFF-ApexNet cited as "Gan et al." when Liong is first author.
> 3. **No out-of-corpus attribution has acquired a bracket.** The `out-of-corpus` rows of `citation_map.tsv` are the authority. Ekman and Friesen, Wu et al., Wadhwa et al., Farnebäck, TV-L1, Pfister et al., Lin et al., SE/CBAM/ECA/GC/SRM, GoogLeNet/VGG16/ResNet-101/DenseNet-169, Yan et al. (2013) on CASME, Polikovsky, Dual-Inception, and the CIFAR/ImageNet-21k/JFT-300M datasets must all remain in author–year form with no number.
> 4. **No in-corpus citation remains in author–year form.**
> 5. **The three known collisions are intact:** `Li et al. [n]` is the Li, X., Hong, X., Moilanen 2018 TAC survey; `Li, Huang and Zhao [n, m]` is Li, Y. 2018 and 2021, two distinct numbers; `Yan et al. [n]` is CASME II 2014 while `Yan et al. (2013)` on CASME carries no number.
> 6. **Multi-source and locator forms are well-formed:** `[7, 8]` not `[7,8]` or `[7][8]`; `Li et al. [6] (Table 6)` not `Li et al. [6, Table 6]`.
>
> ## Output
> - **VERDICT: CLEAN** or **PROBLEMS (n)**
> - **GHOST REFERENCES** — each stated explicitly. Say "none" if none.
> - **NAME/NUMBER MISPAIRINGS** — with the correct value from the reference list
> - **WRONGLY NUMBERED OUT-OF-CORPUS ATTRIBUTIONS**
> - **UNCONVERTED AUTHOR–YEAR CITATIONS**
> - **MALFORMED BRACKETS**
> - **VERIFIED** — a compact list of what you positively confirmed
> - Say explicitly if a category is empty.

---

## Brief 3c — Cross-reference audit *(report only)*

> Auditing the internal cross-references of one thesis section after a formatting edit. **Report only — do NOT edit.**
>
> {{SHARED PREAMBLE}}
>
> ## Inputs
> - File: `{{PATH}}`
> - Sibling sections: `Thesis_Chapter3_LiteratureReview/Tech-wise/[0-9][0-9]_3.*.md`
> - Chapter 2: `Thesis_Chapter2_Background/*.md`
>
> ## Your job
> 1. **Every `§3.x` and `§3.x.y` reference in this section resolves** to a heading that actually exists in one of the sibling section files. List each reference and the heading it resolves to.
> 2. **Every `§2.x` reference resolves** to a heading in `Thesis_Chapter2_Background/`.
> 3. **Every `Table 3.n` reference** resolves to a caption `*Table 3.n — …*` somewhere in the chapter.
> 4. **Every `Figure 3.n` reference** resolves to a caption `*Figure 3.n — …*`, and the reference appears in the prose **before** the figure itself.
> 5. **Figure numbering is sequential chapter-wide** with no gaps and no duplicates.
> 6. **Nothing points at this section's own former headings.** If a reference is broken because a heading was renamed, say so — the fix is to restore the heading, which the writer was forbidden to change, not to repoint the reference.
>
> ## Output
> - **VERDICT: CLEAN** or **BROKEN (n)**
> - **BROKEN SECTION REFERENCES** — the reference, and whether the target ever existed
> - **BROKEN TABLE REFERENCES**
> - **BROKEN FIGURE REFERENCES**
> - **FIGURE ORDER PROBLEMS** — a figure referenced after it appears, or numbering gaps
> - **RENAMED HEADINGS** — any `### 3.x.y` whose title differs from the version at `HEAD`
> - **VERIFIED** — a compact list
> - Say explicitly if a category is empty.
